syntax = "proto3";

package generators;

option go_package = "./generators";

message User {
  string id = 1;
  string name = 2;
  string email = 3;
}

service UserService {
  rpc addUser (User) returns (User);
}